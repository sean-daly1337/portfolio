package service.impl;

import models.requests.ServerLocation;
import org.springframework.stereotype.Service;
import service.MapLocationService;

@Service
public class MapLocationServiceImpl implements MapLocationService {

    @Override
    public ServerLocation getMapLocation(String propertyId) {
        return null;
    }
}